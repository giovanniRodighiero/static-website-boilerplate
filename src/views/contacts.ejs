<%- include('partials/_header', { metatags: contacts.metatags }); %>

<main class="page--contacts">
    <section class="container">
        <div class="row">
            <div class="col-xs-12 col-md-6">
                <h1><%= contacts.title %></h1>
                <p><%= contacts.description %></p>
            </div>
        </div>
    </section>
</main>

<%- include('partials/_footer'); %>
